name: "Cleanup caches by a branch"

on: # yamllint disable-line rule:truthy
    pull_request:
        types:
            - "closed"

jobs:
    cleanup-branch-cache:
        uses: "anolilab/workflows/.github/workflows/cleanup-branch-cache.yaml@0a43f1373be806fe25f390a7119dfd7292c6850f" # v18.0.2 # main
        with:
            target-repo: "anolilab/resume"
