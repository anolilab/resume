name: "Label Idle Issues"

on: # yamllint disable-line rule:truthy
    schedule:
        - cron: "0 0 * * *"

jobs:
    stale-issues:
        uses: "anolilab/workflows/.github/workflows/stale-issues.yml@802f3b1d00b0ab47c3102b4f4ebd866b9f7f8fcf" # v18.0.3 # main
        with:
            target-repo: "anolilab/resume"
